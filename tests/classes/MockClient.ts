import { Client } from '../../src';

/**
 * Class for representing a mock client for testing.
 */
export default class MockClient extends Client {
    /**
     * Constructor for a mock client.
     */
    constructor() {
        super('');
    }
}
